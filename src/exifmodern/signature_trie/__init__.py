"""Signature-trie runtime data structures for file type dispatch.

The production package ships the frozen dispatcher and the small signature
declaration model used by format modules. Source-tree generation tools are
maintainer-only and are excluded from production wheels.
"""
